name: 'my first container action'
description: 'A cloud guru'
author: 'Fabio Mesghali'
inputs:
  myInput:
    description: 'greeting to use'
    required: true
    default: 'Awesome guru'
outputs:
  myOutput:
    description: 'total memory'
runs: 
  using: 'docker'
  image: 'Dockerfile' 
